import {css} from 'lit';

export const styles = css`

// Selector to style the element itself
// The user can override them to customize the appearance
  :host {

  }

  // Selector to style all the slotted children
  ::slotted(*) {
 
  }
`;
